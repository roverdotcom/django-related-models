dist: xenial
language: python
sudo: false
cache: pip
env:
  global:
    - LD_PRELOAD=/lib/x86_64-linux-gnu/libSegFault.so
    - SEGFAULT_SIGNALS=all
  matrix:
    - TOXENV=check
    - TOXENV=docs
matrix:
  include:
    - python: '3.10'
      env:
        - TOXENV=3.10-2.2-cover,report,codecov
    - python: '3.10'
      env:
        - TOXENV=3.10-3.2-cover,report,codecov
    - python: '3.10'
      env:
        - TOXENV=3.10-4.0-cover,report,codecov
    - python: '3.7'
      env:
        - TOXENV=3.7-2.2-cover,report,codecov
    - python: '3.7'
      env:
        - TOXENV=3.7-3.2-cover,report,codecov
    - python: '3.8'
      env:
        - TOXENV=3.8-2.2-cover,report,codecov
    - python: '3.8'
      env:
        - TOXENV=3.8-3.2-cover,report,codecov
    - python: '3.8'
      env:
        - TOXENV=3.8-4.0-cover,report,codecov
    - python: '3.9'
      env:
        - TOXENV=3.9-2.2-cover,report,codecov
    - python: '3.9'
      env:
        - TOXENV=3.9-3.2-cover,report,codecov
    - python: '3.9'
      env:
        - TOXENV=3.9-4.0-cover,report,codecov
before_install:
  - python --version
  - uname -a
  - lsb_release -a
install:
  - pip install tox
  - virtualenv --version
  - easy_install --version
  - pip --version
  - tox --version
script:
  - tox -v
after_failure:
  - more .tox/log/* | cat
  - more .tox/*/log/* | cat
notifications:
  email:
    on_success: never
    on_failure: always
